# Skills

This folder contains reusable skill modules that agents can use. Skills are small, focused scripts (e.g., HTTP requests, math operations) that can be composed into more complex behaviors.

- `http-skill.js`: Example HTTP GET skill.
